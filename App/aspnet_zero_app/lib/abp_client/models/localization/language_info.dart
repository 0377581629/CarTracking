class LanguageInfo {
  String name;
  String displayName;
  String icon;
  bool isDefault;
  bool isDisabled;
  bool isRightToLeft;
  LanguageInfo(this.name, this.displayName, this.icon, this.isDefault,
      this.isDisabled, this.isRightToLeft);
}
