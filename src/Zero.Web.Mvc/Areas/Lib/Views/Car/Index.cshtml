@model Zero.Web.Areas.Lib.Model.Car.CarViewModel
@{
    ViewBag.CurrentPageName = AppPageNames.Lib.Car;
}

@section Scripts
{
    <script abp-src="@ZeroConst.ScriptPathLib/Car/Index.js" asp-append-version="true"></script>
}

<div class="content d-flex flex-column flex-column-fluid" id="kt_content">
    <abp-page-subheader title="@L("Car")" description="@L("CarHeaderInfo")">
        @if (IsGranted(LibPermissions.Car_Create))
        {
            <button id="CreateNewButton" class="btn btn-primary blue">
                <i class="fa fa-plus"></i> @L("Create")
            </button>
        }
    </abp-page-subheader>

    <div class="@(await GetContainerClass())">
        <div class="card card-custom gutter-b">
            <div class="card-body">
                <div class="form">
                    <div id="CarFormFilter">
                        <div class="row mb-15">
                            <div class="col-12">
                                <div class="input-group">
                                    <input id="CarTableFilter" class="form-control" type="text" value="@ViewBag.FilterText" placeholder="@L("SearchWithThreeDot")" aria-describedby="basic-addon2">
                                    <span class="input-group-btn">
                                        <button type="button" name="RefreshButton" class="btn btn-primary">
                                            <i class="fa fa-search"></i>
                                        </button>
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="align-items-center">
                    <table id="CarTable" class="display table table-striped table-bordered table-hover">
                        <thead>
                        <tr>
                            <th></th>
                            <th class="text-center">@L("Actions")</th>
                            <th class="text-center">@L("Code")</th>
                            <th class="text-center">@L("LicensePlate")</th>
                            <th class="text-center">@L("Device")</th>
                            <th class="text-center">@L("CarType")</th>
                            <th class="text-center">@L("CarGroup")</th>
                            <th class="text-center">@L("Driver")</th>
                            <th class="text-center">@L("RfidType")</th>
                        </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>